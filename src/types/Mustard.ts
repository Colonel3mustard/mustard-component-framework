export interface Card {
  title: string;
  subtitle: string;
  imgSrc: string;
}

export interface DropdownItem {
  name: string;
  link: string;
}

export interface NavItem {
  name: string;
  link: string;
  image?: string;
}
